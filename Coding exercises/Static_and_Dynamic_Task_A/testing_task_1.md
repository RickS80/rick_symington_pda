### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')

class CardGame


  def checkforAce(card)
    # should be ==
    if card.value = 1
      return true
    else
      return false
    end
  end

# typo should be 'def' and have a comma between card1 and card2
  dif highest_card(card1 card2)
  if card1.value > card2.value
    # should say card1. .name is not defined.
    return card.name
  else
    # return missing
    card2
  end
end
# additional end not required
end

def self.cards_total(cards)
  # need to add total = 0
  total
  for card in cards
    total += card.value
    # should include string interpolation for + total
    # end is in wrong position
    return "You have a total of" + total
  end
end


# end required at end of file to close off class



```
